# Server port (frontend expects http://localhost:8000)
PORT=8000

# MongoDB connection (local MongoDB or an Atlas connection string)
MONGODB_URI=mongodb+srv://dineshddd154_db_user:Alpha@cluster0.n0uyrvz.mongodb.net/ttpcrm?appName=Cluster0&retryWrites=true&w=majority

# Secret used to sign login tokens. Use a long random string.
# Generate one:  node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=change_me_to_a_long_random_string

# Google Gemini (get a key at https://aistudio.google.com/apikey)
GEMINI_API_KEY=your_gemini_api_key_here
# Optional: model to use first, and fallbacks tried if it is unavailable / rate-limited
# GEMINI_MODEL=gemini-3.8-flash
# GEMINI_FALLBACK_MODELS=gemini-3.7-flash,gemini-3.6-flash

# Optional: extra allowed frontend origins (comma separated). localhost is always allowed.
# CORS_ORIGIN=https://your-site.com
